{
  "name": "ka-env",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "ejs": "^2.3.1",
    "gulp": "^3.8.11",
    "gulp-consolidate": "^0.1.2",
    "gulp-foreach": "^0.1.0",
    "lodash": "^3.3.1"
  }
}
